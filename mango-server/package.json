{
  "name": "mango-server",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "seed": "bun src/seed.ts",
    "test": "bun test src/tests"
  },
  "dependencies": {
    "hono": "^4.12.9",
    "mongodb": "^7.1.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}